<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout

    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:background="@drawable/background"
    android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    tools:context=".MainActivity"
    >

    <TextView
        android:id="@+id/mytext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="#FFFFFF"
        android:textSize="36sp"
        android:layout_centerHorizontal="true"/>

    <ImageView
        android:id="@+id/img_ball"
        android:layout_width="294dp"
        android:layout_height="288dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/imgdsc"
        app:srcCompat="@drawable/ballback" />
        "
        app:srcCompat="@d"@string/imgdsc"
        android:contentDescription="@strin />

    <TextView
        android:id="@+id/txt_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/img_ball"
        android:layout_alignLeft="@id/img_ball"
        android:layout_alignRight="@id/img_ball"
        android:layout_alignTop="@id/img_ball"
        android:gravity="center"
        android:layout_marginTop="-30dp"
        android:text="@string/hello"
        android:textColor="#FFFF00"
        android:layout_alignStart="@id/img_ball"
        android:layout_alignEnd="@id/img_ball" />




</RelativeLayout>
